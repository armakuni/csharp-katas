using System.Reflection;

namespace IDE.Console.Stuff;

internal class GAxSwqww
{

    protected void Udjwkch()
    {
        // "don't"
        var thisAssembly = GetType().Assembly;
        static bool ClassFinder(Type x)
        {
            var os = OOwofjkl.MethodsOf(x);
            return os.Any(m => m.Name == "B2ogjsl");
        }

        var nextType = thisAssembly.GetTypes().First(ClassFinder);
        var next = Activator.CreateInstance(nextType);
        nextType.GetMethod("B2ogjsl", BindingFlags.Instance | BindingFlags.NonPublic)!.Invoke(next, Array.Empty<object>());
    }
}